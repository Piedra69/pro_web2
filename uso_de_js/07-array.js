const ciudad1 = "Sucre";

// definicion simple

const ciudades = new Array("Sucre", "Santa Cruz", "Cochabamba");
console.log(ciudades);

// definicio abreviada

const paises = ["Bolivia", "Paraguay", "Argentina"];
console.log(paises);
{
    // Aumentar elementos al array
    paises.push("Salvador", "Uruguay");

    // Quitar ultimo elemento del array
    paises.pop();
}